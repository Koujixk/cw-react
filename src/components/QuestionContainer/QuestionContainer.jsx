import React from 'react'
import './QuestionContainer.scss'

export const QuestionContainer = (props) => {
    const {isStartWindow} = props
    return (
        <div className='question-wrapper'>
            {isStartWindow && 
                
            }
        </div>
    )
}
